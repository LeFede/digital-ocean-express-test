{
  "name": "test",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "",
    "bunbuild": "bun build src/index.ts --outdir dist --target node --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
